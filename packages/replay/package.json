{
  "name": "@blamr/replay",
  "version": "0.1.0",
  "private": true,
  "description": "LLM hop replay engine for blamr (re-execute causal edges with edited input)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build --workspace=@blamr/types && tsc -p tsconfig.test.json && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "@blamr/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
