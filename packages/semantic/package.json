{
  "name": "@blamr/semantic",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build -w @blamr/types && tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test dist/drift.test.js"
  },
  "dependencies": {
    "@blamr/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
